# TODO

- Подтвердить источник дней 3-7: сейчас по плану используется Open-Meteo как основной/единственный источник для дней 3-7. Если есть `cid` для ДАНИО-пресс или конкретный endpoint Росгидромета, заменить изолированный слой вторичного прогноза.
- Уточнить порог совета про жару: тест Task 3 закрепляет 25°C как «можно гулять», а пример реализации в плане относит `>=25` к жаре. Сейчас реализовано: жара только при температуре выше 25°C.
- Заменить `app/src/test/resources/fixtures/open_meteo.json` на живой ответ Open-Meteo, когда API будет доступен из окружения. Две попытки (`Invoke-WebRequest` и `curl.exe`) 2026-06-25 завершились таймаутом, поэтому сейчас используется офлайн-фикстура той же формы.
- Заменить `app/src/test/resources/fixtures/yandex.json` на живой ответ Яндекс GraphQL с dev-ключом, когда будет целесообразно потратить один запрос лимита. Сейчас используется офлайн-фикстура той же проверенной формы.
- Выполнить ручную проверку Task 10 на устройстве/эмуляторе: GPS должен вернуть координаты, ручной ввод «Казань» через Nominatim должен дать координаты примерно 55.79, 49.12. В текущем окружении `adb devices` не показывает подключённых устройств.
- Выполнить ручную проверку Task 12 на устройстве/эмуляторе: запуск без ключа Яндекса, отображение SVG-иконок офлайн, pull-to-refresh, toast при ошибке, ночные `*_night` иконки. В текущем окружении `adb devices` не показывает подключённых устройств.
- Выполнить ручную проверку Task 13 на устройстве/эмуляторе: светлая/тёмная тема по системе, фон меняется по времени суток, крупные цифры и карточки читаемы. В текущем окружении нет подключённого устройства/эмулятора для визуальной проверки.
- Выполнить ручную проверку Task 14 на устройстве/эмуляторе: родительский замок, очистка ключа Яндекса приводит к прогнозу только Open-Meteo, валидный ключ включает Яндекс для «сейчас»/почасового прогноза, смена города и режима GPS↔ручной обновляет главный экран. В текущем окружении нет подключённого устройства/эмулятора и не проверялись OkHttp-логи.
